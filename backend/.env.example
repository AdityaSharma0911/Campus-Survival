# Paste your Google AI Studio key: https://aistudio.google.com/api-keys
# Leave blank to run on the rule-based parser (the demo still works).
GEMINI_API_KEY=

# Optional
GEMINI_MODEL=gemini-flash-latest
PORT=8787

# DEMO_MODE=1 freezes the clock at Wednesday 12:15 PM so the demo is
# reproducible even when the Campus Center is closed.
# DEMO_MODE=1
