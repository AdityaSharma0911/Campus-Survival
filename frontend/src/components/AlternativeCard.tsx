import {Coffee,Pizza,BookOpen,ArrowUpRight} from 'lucide-react';
export default function AlternativeCard({name,detail,status,onClick}:{name:string;detail:string;status:string;onClick:()=>void}){const Icon=name==='Gateway Café'?Coffee:name==='Nearby Food'?Pizza:BookOpen;return <button className="alternative" onClick={onClick}><span className="alt-icon"><Icon size={20}/></span><div><strong>{name}</strong><small>{detail}</small></div><span className={'alt-status '+(status==='TIGHT'?'tight':status==='TOO RISKY'?'screwed':'safe')}>{status}</span><ArrowUpRight size={16}/></button>}
