import {Zap,X,Check,ArrowRight} from 'lucide-react';
export default function ScrewedMode({onDirect,onClose}:{onDirect:()=>void;onClose:()=>void}){return <section className="emergency-panel"><div className="section-line"><span className="emergency-badge"><Zap size={15}/> I'M SCREWED</span><button aria-label="Close emergency mode" onClick={onClose}><X size={20}/></button></div><h2>You have <em>8 minutes.</em><br/>Let's make the right call.</h2><div className="emergency-options">{[['Campus Café','5 min walk + 10 min eating + 6 min to class','21 min'],['Nearby Restaurant','8 min walk + 12 min eating + 6 min to class','26 min'],['Go directly to class','6 min walk · 2 minute buffer','6 min']].map(([name,detail,time],i)=><div key={name} className={i===2?'direct-option':''}>{i===2?<Check size={18}/>:<X size={18}/>}<div><strong>{name}</strong><small>{detail}</small></div><b>{time}</b></div>)}</div><div className="your-move"><div><span className="eyebrow">YOUR MOVE</span><h3>RUN TO CLASS 🏃</h3><p>Maybe eat after class. You're on the clock.</p></div><button className="primary" onClick={onDirect}>Take direct route <ArrowRight size={17}/></button></div></section>}
