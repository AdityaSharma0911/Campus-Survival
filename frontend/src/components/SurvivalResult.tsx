import {Check,ArrowUpRight,Sandwich,Clock3} from 'lucide-react';
import type {SurvivalRecommendation} from '../types/campus';
import RiskIndicator from './RiskIndicator';
export default function SurvivalResult({result,onRoute}:{result:SurvivalRecommendation;onRoute:()=>void}){return <section className="result-card"><div className={'result-status '+result.risk}><span className="check-circle"><Check size={14}/></span>{result.risk==='safe'?'YOU CAN MAKE IT':result.risk==='tight'?'CUTTING IT CLOSE':'SKIP THE DETOUR'}</div><h2>{result.risk==='safe'?'Good timing. Great plan.':result.risk==='tight'?'You can make it. Just.':'Class comes first.'}</h2><p>{result.risk==='screwed'?"This stop won't fit. Take the direct 6-minute route to class.":`You have enough time for ${result.location==='University Library'?'a study break':result.location==='Innovation Hall'?'the walk':result.location==='Gateway Café'?'coffee':'food'} and your next class.`}</p><div className="recommendation"><span className="food-icon"><Sandwich size={25}/></span><div><span className="eyebrow">{result.risk==='screwed'?'SELECTED OPTION':'YOUR BEST MOVE'}</span><h3>{result.location}</h3><small><span className="tiny-dot"/> Open in demo <span>·</span> On your route</small></div><span className="best-tag">{result.risk==='screwed'?'Too far':'Best option'}</span></div><div className="result-bottom"><RiskIndicator risk={result.risk} buffer={result.bufferMinutes}/><div className="total"><Clock3 size={17}/><b>{result.totalMinutes} minutes</b><span>Total trip time</span><button onClick={onRoute}>VIEW ROUTE <ArrowUpRight size={15}/></button></div></div></section>}
