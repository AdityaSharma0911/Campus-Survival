import {Zap,UserRound,MapPin} from 'lucide-react';
export default function Header({view,onView}:{view:string;onView:(v:string)=>void}){return <header><div className="header-inner"><a className="brand" href="#" onClick={e=>{e.preventDefault();onView('Survive')}}><span className="brand-icon"><Zap size={22} fill="currentColor"/></span><span>CAMPUS<span className="brand-second">SURVIVAL</span></span></a><span className="campus-label"><MapPin size={12}/> INDIANAPOLIS CAMPUS</span><nav>{['Survive','Campus','Places'].map(v=><button key={v} className={view===v?'nav-active':''} onClick={()=>onView(v)}>{v}</button>)}</nav><span className="avatar" title="Student demo profile" aria-label="Student demo profile"><UserRound size={18}/></span></div></header>}
