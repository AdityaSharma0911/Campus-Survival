import {ArrowUpRight,Sparkles,Play,Zap} from 'lucide-react';
export default function SearchBox({query,setQuery,onSubmit,onDemo,onEmergency,loading}:{query:string;setQuery:(s:string)=>void;onSubmit:()=>void;onDemo:()=>void;onEmergency:()=>void;loading:boolean}){return <><form className="search-box" onSubmit={e=>{e.preventDefault();if(query.trim())onSubmit()}}><Sparkles className="search-spark" size={22}/><textarea aria-label="Your situation" value={query} onChange={e=>setQuery(e.target.value)} placeholder="I have 25 minutes between CS 180 and Physics and I'm hungry..." rows={2} onKeyDown={e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();if(query.trim()&&!loading)onSubmit()}}}/><button className="primary" disabled={!query.trim()||loading}>SURVIVE <ArrowUpRight size={19}/></button></form><div className="search-meta"><span><span className="tiny-dot"/> A little planning. A lot less panic.</span><div><button onClick={onDemo} disabled={loading}><Play size={13}/> Try Demo</button><span className="divider"/><button className="emergency-link" onClick={onEmergency} disabled={loading}><Zap size={14}/> I'm Screwed</button></div></div></>}
